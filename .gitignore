# =====================================================
# CRITICAL: Secrets - NUNCA commits estos archivos
# =====================================================
.env
.env.*
!.env.example
config/*.key
config/*.p12
*.key
secrets/
.kalshi-keys/

# =====================================================
# Python
# =====================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
build/
dist/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/

# Virtual environments
venv/
env/
ENV/
.venv/
.env/

# =====================================================
# Testing & coverage
# =====================================================
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/
nosetests.xml

# =====================================================
# Type checking
# =====================================================
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/

# =====================================================
# IDE / Editors
# =====================================================
.vscode/
.idea/
*.swp
*.swo
*~
.project
.pydevproject

# =====================================================
# OS
# =====================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# =====================================================
# Bot data y logs (persisten en volumes Docker, no Git)
# =====================================================
data/
logs/
backups/
*.db
*.db-journal
*.sqlite
*.sqlite3

# =====================================================
# Docker
# =====================================================
docker-compose.override.yml

# =====================================================
# Jupyter (si lo usamos para análisis offline)
# =====================================================
.ipynb_checkpoints/
*.ipynb

# =====================================================
# Claude Code (worktrees efímeros de agentes, estado local)
# =====================================================
# OJO: `.claude/` entero ignoraba TAMBIÉN las skills — 8 skills de motores creadas en
# jul-2026 nunca llegaron al repo (vivían solo en el workspace del agente) y el operador
# no las veía en GitHub. Las skills SON parte del proyecto (protocolo + una por motor);
# el resto de .claude/ (estado local, settings) sigue ignorado.
.claude/*
!.claude/skills/
